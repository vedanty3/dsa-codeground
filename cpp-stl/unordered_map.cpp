#include <iostream>
#include <unordered_map>
using namespace std;

/*
int main()
{
    unordered_map<int, string> m; // uses hash table for implementation
    m[1] = "vedant";              // O(1)
    m[2] = "yetekar";
    m[22] = "hello";
    m[109] = "world";
    m.erase(2);
    for (auto &x : m) // O(1)
    {
        cout << x.first << " - " << x.second << endl;
    }
    cout << endl;

    return 0;
}
*/

/*
int main()
{
    unordered_map<string, int> m;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        string s;
        cin >> s;
        m[s]++;
    }

    int q;
    cin >> q;
    while (q--)
    {
        string s;
        cin >> s;
        cout << m[s] << endl;
    }

    return 0;
}
*/